# Secrets
.env
.env.local
.env.*.local
.env.failed-token

# Build artefacts (regenerated by build.sh, no longer used in v4 but keep ignored)
worker.bundle.js

# Wrangler runtime cache + locally-installed wrangler binary tree
.wrangler/
.wrangler-tools/
node_modules/

# macOS / editor cruft
.DS_Store
.idea/
.vscode/
*.swp

# Logs
*.log

# Cloudflare Workers types (if generated locally)
worker-configuration.d.ts
